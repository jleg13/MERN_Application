// const { validationResult } = require("express-validator");
// const restaurantModel = require("../models/restaurant");

exports.read_bookinglist_list = (req, res) => {
  res.status(200).json({ message: "Hello World!" });
};

